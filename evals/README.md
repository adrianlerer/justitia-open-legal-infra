# Verification Cases

Synthetic cases for checking whether a legal AI workflow detects known issues.

Each case should have:

1. `input.md` — anonymized or synthetic legal material.
2. `rubric.md` — issues the system should identify.
3. `expected.md` — expected findings or answer shape.

Do not include real client documents unless they are public, anonymized and safe
to redistribute.
